<!doctype html>
<html lang="de">
<head>
<meta charset="utf-8" />
<meta name="viewport" content="width=device-width, initial-scale=1" />
<title>Rave Camp 2024 — Rave3eN</title>
<meta name="description" content="Erlebe das Rave Camp 2024 von Rave3eN – Musik, Emotionen, Gemeinschaft und unvergessliche Nächte." />

<link href="https://fonts.googleapis.com/css2?family=Orbitron:wght@400;700&family=Rajdhani:wght@300;500;700&display=swap" rel="stylesheet">
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css">

<style>
:root{
  --bg:#000;
  --neon1:#a85cff;
  --neon2:#007bff;
  --neon3:#00eaff;
}
*{box-sizing:border-box;margin:0;padding:0}
body{
  font-family:'Rajdhani',system-ui,Segoe UI,Roboto,Arial;
  background:#000;
  color:#e9eef8;
  overflow-x:hidden;
}
header{
  position:fixed;top:0;left:0;width:100%;
  padding:15px 40px;
  display:flex;align-items:center;justify-content:space-between;
  background:rgba(0,0,0,0.4);
  backdrop-filter:blur(10px);
  box-shadow:0 0 25px rgba(0,0,0,0.8);
  z-index:10;
}
header .logo{
  display:flex;align-items:center;gap:28px;
}
header .logo img{height:45px;filter:drop-shadow(0 0 12px var(--neon2)) drop-shadow(0 0 20px var(--neon1));}
header .logo span{
  font-family:'Orbitron';font-size:1.9rem;font-weight:700;
  background:linear-gradient(90deg,var(--neon1),var(--neon2),var(--neon3),var(--neon1));
  background-size:400%;
  -webkit-background-clip:text;-webkit-text-fill-color:transparent;
  animation:gradientFlow 6s linear infinite;
}
@keyframes gradientFlow{0%{background-position:0% 50%}50%{background-position:100% 50%}100%{background-position:0% 50%}}
main{
  margin-top:120px;
  text-align:center;
  padding:30px;
}
h1{
  font-family:'Orbitron';
  font-size:2.3rem;
  color:var(--neon1);
  margin-bottom:20px;
  text-shadow:0 0 15px rgba(155,92,255,0.6);
}
.story{
  max-width:900px;
  margin:20px auto;
  line-height:1.8;
  color:#cfd8f6;
  font-size:1.1rem;
}
.gallery{
  margin:50px auto;
  display:flex;
  flex-direction:column;
  gap:40px;
  align-items:center;
}
.gallery img{
  max-width:100%;
  width:95%;
  height:auto;
  border-radius:18px;
  object-fit:contain;
  box-shadow:0 0 45px rgba(155,92,255,0.5),0 0 90px rgba(0,210,255,0.3);
  opacity:0;
  transform:translateY(50px) scale(1.05);
  transition:1s ease;
}
.gallery img.visible{
  opacity:1;
  transform:translateY(0) scale(1);
}
a.back{
  display:inline-block;
  margin:50px auto 40px;
  color:var(--neon2);
  text-decoration:none;
  font-weight:600;
}
a.back:hover{text-decoration:underline;color:var(--neon1);}
canvas#waves{position:fixed;top:0;left:0;width:100%;height:100%;z-index:0;}
</style>
</head>
<body>
<canvas id="waves"></canvas>

<header>
  <div class="logo">
    <img src="logo druck.png" alt="Rave3eN Logo">
    <span>Rave3eN</span>
  </div>

  <nav>
    <a href="index.html">Home</a>
    <a href="about.html">About Me</a>
    <a href="events.html">Events</a>
  </nav>
</header>

<main>
  <h1>Rave Camp 2024</h1>
  <p class="story">
    Das <strong>Rave Camp 2024</strong> war ein Wochenende voller Energie, Musik und echter Emotionen.  
    Kein Festival – ein Gefühl. Jeder Beat, jede Nacht, jedes Gespräch unter Neonlicht hat uns verbunden.  
    Das war mehr als ein Camp. Das war Rave3eN. 💜
  </p>

  <div class="gallery">
    <img src="camp1.jpg" alt="Rave Camp Bild 1">
    <img src="camp2.jpg" alt="Rave Camp Bild 2">
    <img src="camp3.jpg" alt="Rave Camp Bild 3">
  </div>

  <a href="events.html" class="back">← Zurück zu Events</a>
</main>

<script>
const imgs=document.querySelectorAll('.gallery img');
function reveal(){
  imgs.forEach(img=>{
    const rect=img.getBoundingClientRect();
    if(rect.top<window.innerHeight-100){img.classList.add('visible');}
  });
}
window.addEventListener('scroll',reveal);
reveal();

/* WAVES */
const canvas=document.getElementById('waves'),ctx=canvas.getContext('2d');
let w,h,t=0;const parts=[];
function resize(){w=canvas.width=innerWidth;h=canvas.height=innerHeight;}
addEventListener('resize',resize);resize();
class P{constructor(){this.x=Math.random()*w;this.y=Math.random()*h;this.vx=(Math.random()-.5)*.2;this.vy=(Math.random()-.5)*.2;this.s=1+Math.random()*2;this.a=.3+Math.random()*.3;}u(){this.x+=this.vx;this.y+=this.vy;if(this.x<0)this.x=w;if(this.x>w)this.x=0;if(this.y<0)this.y=h;if(this.y>h)this.y=0;}d(){ctx.beginPath();ctx.globalAlpha=this.a;ctx.fillStyle='#b5e4ff';ctx.arc(this.x,this.y,this.s,0,2*Math.PI);ctx.fill();ctx.globalAlpha=1;}}
for(let i=0;i<80;i++)parts.push(new P());
function wave(c,y,a,f,s,o){ctx.beginPath();for(let x=0;x<w;x++){const yy=y+Math.sin(x*f+t*s+o)*a;ctx.lineTo(x,yy);}ctx.strokeStyle=c;ctx.lineWidth=2;ctx.shadowBlur=20;ctx.shadowColor=c;ctx.stroke();}
function anim(){ctx.fillStyle='#000';ctx.fillRect(0,0,w,h);wave('rgba(90,120,255,0.65)',h*0.3,40,0.008,0.018,0);wave('rgba(155,92,255,0.9)',h*0.55,30,0.01,0.022,2.5);wave('rgba(0,255,255,0.8)',h*0.75,20,0.013,0.026,5.0);parts.forEach(p=>{p.u();p.d();});t+=0.02;requestAnimationFrame(anim);}anim();
</script>
</body>
</html>
